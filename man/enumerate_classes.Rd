% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/enumeration.R
\name{enumerate_classes}
\alias{enumerate_classes}
\title{Enumerate numbers of individual profiles and group classes}
\usage{
enumerate_classes(
  data,
  indicators,
  group,
  profiles = 1:4,
  group_classes = 1:3,
  seed = NULL,
  ...
)
}
\arguments{
\item{data}{Data frame.}

\item{indicators}{Continuous indicator names.}

\item{group}{Group identifier column name.}

\item{profiles}{Positive integer profile counts to try.}

\item{group_classes}{Positive integer group-class counts to try.}

\item{seed}{Optional reproducible seed for each fit.}

\item{...}{Further arguments to [multilpa()].}
}
\value{
An object of class `multilpa_enumeration`. Read it with the verbs
  that describe it rather than by reaching into it: [as.data.frame()] gives
  one row per candidate model with every criterion and diagnostic,
  [summary()] gives one row per information criterion naming the candidate
  that minimises it, [plot()] draws one criterion across the grid, and
  [candidate_fit()] returns the fitted model for one cell of the grid.
}
\description{
Fits every requested combination and retains errors and warnings alongside
successful fits. Information criteria are descriptive: neither the smallest
BIC nor high entropy guarantees the correct number of classes. Unconverged,
boundary, and unreplicated fits are reported, not silently selected.
}
\examples{
set.seed(1)
d <- data.frame(g = rep(1:10, each = 10), y = rnorm(100))
candidates <- enumerate_classes(d, "y", "g", profiles = 1:2,
                              group_classes = 1, n_starts = 2, seed = 1)
as.data.frame(candidates)
summary(candidates)
}
\seealso{
[candidate_fit()] to take one fitted model out of the grid,
  [summary.multilpa_enumeration()] for the criterion-by-criterion comparison.
}
