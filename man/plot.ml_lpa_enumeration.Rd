% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot.R
\name{plot.ml_lpa_enumeration}
\alias{plot.ml_lpa_enumeration}
\title{Plot a class-enumeration grid}
\usage{
\method{plot}{ml_lpa_enumeration}(
  x,
  criterion = "bic_individual",
  labels = TRUE,
  mark_minimum = TRUE,
  main = NULL,
  subtitle = NULL,
  palette = NULL,
  symbols = NULL,
  linetypes = NULL,
  style = .ml_lpa_style(),
  ...
)
}
\arguments{
\item{x}{An `ml_lpa_enumeration` result from [enumerate_ml_lpa()].}

\item{criterion}{Name of the column to plot, as it appears in
`as.data.frame(x)`, for example `"bic_individual"` or `"sabic_groups"`.}

\item{labels}{`TRUE` prints a direct label at the right end of each series.}

\item{mark_minimum}{`TRUE` rings the lowest value of the criterion. This
marks an extremum, it does not select a model.}

\item{main, subtitle}{Panel title and secondary line.}

\item{palette, symbols, linetypes}{Series aesthetics, recycled over the number
of group-class counts.}

\item{style}{A list of visual constants, as built by `.ml_lpa_style()`.}

\item{...}{Further named visual constants, merged into `style`.}
}
\value{
The enumeration result, invisibly. Called for its drawing side effect.
}
\description{
Draws one information criterion against the number of profiles, with one
line per number of group classes. Candidates that failed to converge are
marked rather than dropped, so a gap in a line is visible as a failure and
not mistaken for a missing candidate.
}
\examples{
set.seed(7)
example_data <- data.frame(
  school = rep(seq_len(12), each = 10),
  score_a = rnorm(120), score_b = rnorm(120)
)
candidates <- enumerate_ml_lpa(example_data, c("score_a", "score_b"), "school",
                               profiles = 1:3, group_classes = 1, n_starts = 2,
                               seed = 1)
plot(candidates)
}
