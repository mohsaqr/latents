% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/covariates.R
\name{print.multilpa_covariates}
\alias{print.multilpa_covariates}
\title{Print a covariate LPA fit}
\usage{
\method{print}{multilpa_covariates}(x, ...)
}
\arguments{
\item{x}{A covariate LPA fit.}

\item{...}{Reserved.}
}
\value{
The model, invisibly. Called for the side effect of printing the
  class counts, the covariate counts, the log likelihood with the
  information criteria, and the convergence diagnostics.
}
\description{
Print a covariate LPA fit
}
\examples{
set.seed(1)
example_data <- data.frame(group = rep(seq_len(20), each = 10),
                           z = rnorm(200))
example_data$y <- rnorm(200,
  ifelse(runif(200) < plogis(example_data$z), -3, 3))
fit <- fit_covariates(example_data, "y", "group", n_profiles = 2,
                      n_group_classes = 1, profile_covariates = "z",
                      n_starts = 2, seed = 1)
print(fit)
}
