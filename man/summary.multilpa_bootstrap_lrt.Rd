% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/enumeration.R
\name{summary.multilpa_bootstrap_lrt}
\alias{summary.multilpa_bootstrap_lrt}
\title{Summarise a parametric bootstrap likelihood-ratio comparison}
\usage{
\method{summary}{multilpa_bootstrap_lrt}(object, ...)
}
\arguments{
\item{object}{An \code{multilpa_bootstrap_lrt} result.}

\item{...}{Reserved for compatibility with \code{summary()}.}
}
\value{
An object of class \code{summary_multilpa_bootstrap_lrt}, with a \code{print}
method and an \code{\link[=as.data.frame]{as.data.frame()}} accessor. \code{as.data.frame()} returns the
one-row test table by default and one row per replicate with
\code{what = "replicates"}.
}
\description{
Summarise a parametric bootstrap likelihood-ratio comparison
}
\examples{
set.seed(1)
example_data <- data.frame(
  school = rep(seq_len(10), each = 10),
  score = rnorm(100, rep(c(-2, 2), each = 50))
)
smaller <- multilpa(example_data, "score", "school", n_profiles = 1,
                    n_group_classes = 1, n_starts = 2, seed = 1)
larger <- multilpa(example_data, "score", "school", n_profiles = 2,
                   n_group_classes = 1, n_starts = 2, seed = 1)
# `iter` is small so the example runs quickly; use many more for inference.
comparison <- bootstrap_lrt(smaller, larger, iter = 9, n_starts = 1,
                            max_iter = 2000, tol = 1e-6, seed = 1)
summary(comparison)
}
