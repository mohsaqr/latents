% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/enumeration.R
\name{as.data.frame.summary_multilpa_bootstrap_lrt}
\alias{as.data.frame.summary_multilpa_bootstrap_lrt}
\title{Coerce a bootstrap comparison summary to its primary table}
\usage{
\method{as.data.frame}{summary_multilpa_bootstrap_lrt}(x, row.names = NULL, optional = FALSE, ...)
}
\arguments{
\item{x}{An object of class \code{summary_multilpa_bootstrap_lrt}.}

\item{row.names}{Passed to \code{data.frame()}; \code{NULL} gives default row names.}

\item{optional}{Ignored, present for generic compatibility.}

\item{...}{Must be empty. An argument here raises \code{latents_bad_argument}
naming it, rather than being dropped.}
}
\value{
A base \code{data.frame}: the one-row test result.
}
\description{
Plain coercion, as the base generic means it: one object, one data frame.
A summary carries every table the object it describes can produce, and
\code{\link[=get_results]{get_results()}} names them.
}
\examples{
set.seed(1)
example_data <- data.frame(
  school = rep(seq_len(10), each = 10),
  score = rnorm(100, rep(c(-2, 2), each = 50))
)
smaller <- multilpa(example_data, "score", "school", n_profiles = 1,
                    n_group_classes = 1, n_starts = 2, seed = 1)
larger <- multilpa(example_data, "score", "school", n_profiles = 2,
                   n_group_classes = 1, n_starts = 2, seed = 1)
# `iter` is small so the example runs quickly; use many more for inference.
comparison <- bootstrap_lrt(smaller, larger, iter = 9, n_starts = 2,
                            max_iter = 2000, tol = 1e-6, seed = 1)
as.data.frame(summary(comparison))
}
\seealso{
\code{\link[=get_results]{get_results()}} for every other table this summary holds.
}
