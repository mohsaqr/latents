% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/accessors.R
\name{print.multilpa_start}
\alias{print.multilpa_start}
\title{Print a set of starting values}
\usage{
\method{print}{multilpa_start}(x, ...)
}
\arguments{
\item{x}{A `multilpa_start` object from [starting_values()].}

\item{...}{Reserved for compatibility with `print()`.}
}
\value{
The input, invisibly.
}
\description{
Print a set of starting values
}
\examples{
set.seed(7)
example_data <- data.frame(
  school = rep(seq_len(12), each = 10),
  score_a = rnorm(120), score_b = rnorm(120)
)
fit <- multilpa(example_data, c("score_a", "score_b"), "school",
                  n_profiles = 2, n_group_classes = 1, n_starts = 2, seed = 1)
print(starting_values(fit))
}
\seealso{
[get_data()] for the values as tidy tables.
}
