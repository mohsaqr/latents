# Run this file from the project root to load the independent numerical oracles.
# The implementation lives alongside its tests so R CMD check can use it too.
source(file.path("tests", "testthat", "helper-independent-likelihood.R"))
